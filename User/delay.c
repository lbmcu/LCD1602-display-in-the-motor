#include "delay.h"


void Delay(uint i)
{
    uchar x, j;
    for(j = 0; j < i; j++)
        for(x = 0; x <= 148; x++);
}

//不用，注释掉，防止报错未调用该函数
/********************************************************************
* 名称 : Delay()
* 功能 : 延时,延时时间大概为1毫秒
* 输入 : 无
* 输出 : 无
***********************************************************************/
//void Delay_1ms(uint i)
//{
//    uint x, j;
//    for(j = 0; j < i; j++)
//        for(x = 0; x <= 148; x++);
//}


/********************************************************************
* 名称 : delay()
* 功能 : 延时,延时时间大概为140US。
* 输入 : 无
* 输出 : 无
***********************************************************************/

//不用注释掉，防止报错未调用该函数
//void Delay2()
//{
//    int i, j;
//    for(i = 0; i <= 10; i++)
//        for(j = 0; j <= 2; j++)
//            ;
//}

///*******************************************************************************
//* 函 数 名         : delay
//* 函数功能		   : 延时函数，i=1时，大约延时10us
//*******************************************************************************/
//void delay1(uchar i)
//{
//    while(i--);
//}

/* 名称 : delay()
* 功能 : 延时,延时时间大概为5US。
* 输入 : 无
* 输出 : 无
***********************************************************************/

void delay3()
{
    _nop_();
    _nop_();
    _nop_();
    _nop_();
    _nop_();
}